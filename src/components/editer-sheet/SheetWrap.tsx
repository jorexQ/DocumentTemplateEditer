import * as React from 'react';

interface SheetWrapProps {
};

interface SheetWrapState {};

class SheetWrap extends React.Component<SheetWrapProps, SheetWrapState> {
    public render(): JSX.Element {
        return (<span>ComponentName</span>);
    }
}

export default SheetWrap;
